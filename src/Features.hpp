#ifndef FEATURES_HPP
#define FEATURES_HPP

// Scope is totally broken on MAC OS only. So use old scope for MAC.
// v1.0 : New scope is OK for MAC.
#define USE_NEW_SCOPE	1 //(!(__APPLE__))

#endif // FEATURES